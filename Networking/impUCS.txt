unified computing system : 
provide compute capability, network capability with very little cabling
you can deploy your shifts ver quikly, and install it without to have length in cabaling
networking, storage, compute,data center.